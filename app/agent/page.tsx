"use client";
import {useState} from "react";
import {Terminal,ShieldCheck,Copy,CheckCircle2} from "lucide-react";
export default function Agent(){const [copied,setCopied]=useState(false);const command="python3 wifi_audit_agent.py --bind 127.0.0.1 --port 8787";return <main className="wrap"><nav className="nav"><div className="brand"><div className="brandIcon"><Terminal size={21}/></div>Local Agent Setup</div><div className="badge">LOCALHOST ONLY</div></nav><section className="panel"><div className="eyebrow">Agent architecture</div><h1 style={{fontSize:"clamp(36px,5vw,60px)",margin:"12px 0"}}>Keep Wi‑Fi adapter access on your machine.</h1><p className="muted" style={{fontSize:16}}>The web dashboard should never receive your wireless credentials or raw adapter access. A future local agent can collect authorized configuration evidence and send sanitized results to this dashboard.</p><div className="report" style={{marginTop:20}}>{command}</div><div className="actions"><button className="button" onClick={()=>{navigator.clipboard?.writeText(command);setCopied(true)}}><Copy size={16}/>{copied?"Copied":"Copy command"}</button></div></section><section className="grid"><div className="card"><ShieldCheck size={20}/><h3>Local-only binding</h3><div className="muted">Agent listens on localhost rather than exposing your adapter to the internet.</div></div><div className="card"><CheckCircle2 size={20}/><h3>Evidence-first</h3><div className="muted">Collect configuration and test evidence before producing a report.</div></div><div className="card"><Terminal size={20}/><h3>Separate execution</h3><div className="muted">Browser UI remains separate from privileged wireless operations.</div></div></section><div className="footer">The repository currently contains the dashboard architecture only; no wireless attack executable is bundled.</div></main>}